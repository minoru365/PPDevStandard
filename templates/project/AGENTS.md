# Power Platform AI 開発 overlay

このファイルは、AI がこのプロジェクトを安全に扱うための共通ルールの土台です。最初に `<...>` をプロジェクトの実際の情報へ置き換えてください。既存の `AGENTS.md` がある場合、初期化ツールは上書きしません。このファイルの見出し単位で、既存ルールへ手動マージしてください。

## このプロジェクトで最初に確認すること

- **正本**: `<Canvas 成果物 / Solution / YAML / フロー定義の場所>`
- **開発環境**: `<操作してよい開発環境の識別方法。URL・資格情報は書かない>`
- **検証**: `<変更後に実行するコマンドと手動確認>`
- **承認が必要な操作**: `<公開・import・削除など、チーム固有の条件>`

プロジェクトの正本、デプロイ手順、CI/CD、承認ルールはこの記入欄とプロジェクト固有の文書で定めます。このテンプレートはそれらを置き換えません。

FlowStudio は外部有償 MCP のため、この共通構成の設定・依存関係には含めない。

## ツールの使い分け

| 作業 | 第一候補 | 採用後の正本 |
| --- | --- | --- |
| Power Automate の探索・新規試作・失敗診断 | 公式 FlowAgent | プロジェクトのフロー定義または Solution |
| 既存フローの変更 | プロジェクト固有の正本 | プロジェクト固有の正本 |
| Dataverse の調査 | Dataverse Skills / MCP | プロジェクトのスキーマ・Solution・スクリプト |
| Canvas Apps の試作 | Canvas Authoring MCP | 版管理された Canvas 成果物 |
| Code Apps の実装・ローカル実行・データソース追加 | 公式 Code Apps skills と PAC CLI | アプリのソースとデプロイ手順 |
| Copilot Studio の作成・検証・評価 | 公式 YAML skills | Git 管理された YAML |
| フロー・Code Apps の品質確認 | Power CAT | レビュー結果とプロジェクトの正本 |
| 現在の公開仕様・制限・公式コード例の確認 | Microsoft Learn MCP | 公式 Learn の根拠とリンク |

製品仕様、非推奨化、機能制限、公式コード例に依存する判断では、まず Microsoft Learn MCP で公式知識を確認する。Microsoft Learn MCP は公開ドキュメントだけを読む標準知識源であり、テナント、Solution、アプリの実態は示さない。その文脈が必要な場合だけ、許可された開発環境の Dataverse MCP、Canvas Authoring MCP、FlowAgent、PAC MCP を使う。

## 探索・試作

開発環境でのみ、新規かつ一時的な資産を MCP / AI スキルで作成・検証できる。FlowAgent で作る新規フローは停止状態にする。Dataverse はメタデータ確認と読取りを既定にする。データまたはスキーマを書き込む試作では、対象と戻し方を先に示す。

FlowAgent の新規フローは、開発環境で停止状態に作成し、`validate_flow` と `preflight_flow`、接続参照のレビューを通す。既存 Solution の接続参照を優先し、接続を自動作成しない。有効化、公開、Solution import は対象と影響を示した明示承認後に行う。

既存の管理対象を置換、削除、公開、import、変更しない。

## 別環境への移送

移送物が**移送元の環境固有の値**を含んでいないかを import 前に確認する。**同じソリューションが、片方の環境でだけ入らないことがある。** 権限の深度可否、接続と接続参照、環境変数、ID 参照が典型。

移送先の状態は移送元の記述で判断しない。import の前に移送先の現状（フローの有効・無効、接続参照の紐付け、ロール割り当てなど、ソリューションに含まれない設定）を控え、import 後に突き合わせる。失敗した import が途中まで適用されていないかも、控えた内容と比べて確かめる。

## 採用・運用

共有、継続保守、複数環境展開、業務影響、データまたはセキュリティに関わる変更は、Git 管理されたプロジェクトの Python、Solution、YAML、フロー定義、版管理 Canvas 成果物、CI/CD へ反映する。適用前に差分、接続参照、依存関係、検証結果をレビューする。

正本を移したら、同じ対象を書ける旧経路をその場で塞ぐ。両方が書ける状態を残すと、片方が「別経路で作られたものを消す」「あとから上書きする」側になる。冪等性のために全削除して作り直すスクリプトは、正本が移った時点で破壊操作に変わる。

新しいガードを追加しても、旧経路が生きている限り迂回される。経路を1つ足すのではなく、古い経路を閉じるところまでを1つの変更として扱う。

既存資産の変更、公開、削除、セキュリティロール、環境設定、Solution import、本番操作は、対象と影響を示した明示承認後だけに行う。

## 検証

テストが落ちたら、まずテストの主張が正しいか読む。実装を直したのにテストが落ちたなら、テストが仕様ではなくバグの形を固定している可能性を検討する。実装を書いた後に足したテストは「こう動くべき」ではなく「今こう動いている」を写経しやすい。

正本を別の場所へ移したら、テストの参照先も移す。移し忘れると、テストは「もうデプロイされないもの」を検査したまま緑になる。落ちないので誰も気づかない。テストが読んでいるのは実際にデプロイされる成果物か、を確認する。

「テストが通った」と報告するときは、そのテストが何を守っていないかも書く。画面の描画と操作、外部サービスの呼び出し、クラウド側の状態は自動テストの対象外であることが多く、そこは実機確認だけが根拠になる。

クラウド側の変更は、成果物の確認と挙動の確認を分けて報告する。レコードができた・YAML が通った・文字列が反映されたは、成果物の確認にすぎない。意図どおり動くかは、実際に動かした結果だけが根拠になる。

リポジトリ内の文書を根拠として引く前に、その記述に出典があるかを見る。出典の無い記述は、設計時の推測がそのまま手順書へ流れたものでありうる。同じリポジトリの別の文書が、同じ論点を「未確認」「要検証」と書いていないかも確かめる。書いてあれば、その文書が指定する確かめ方に従う。文書は書いた時点のスナップショットであり、一次情報ではない。

出典の無い記述を疑った結果として据えるのは「未確定」であり、「否定」ではない。**公式ドキュメントに記載が無いことは、不要であることを意味しない。** 制約や前提の一覧は網羅を約束しておらず、別の製品側で定義された要件を再掲するとも限らない。記載が見つからなかった時点で止めて未確定と述べ、否定を主張するなら否定側の証拠を出す。必要な権限に関する主張は、その設定を外した状態で実際に呼び出して確かめる。

推測を文書へ書くときは、推測と分かる形で書く。断定された文は、後から読む人も別の AI も疑わない。

機能ごとの既知の落とし穴は PPDevStandard の `profiles/capabilities.json` の `knownPitfalls` にある。作業前に該当機能を確認する。

## 秘密情報

テナント URL、アカウント、認証情報、認証キャッシュ、ユーザー固有の Dataverse MCP 接続設定は、リポジトリや共有レポートに書かない。
